'use client';
import { useState } from 'react';
import { useRouter, useSearchParams } from 'next/navigation';
import { AppButton, AppInput, AppSelect, AppTextarea, FieldError, FormErrorSummary } from '@/components/ui/primitives';
import { AppModal, ConfirmDialog } from '@/components/ui/dialogs';
import { useToast } from '@/components/ui/toast';

type ApiResult = { success: boolean; formError?: string; fieldErrors?: Record<string, string[]>; data?: { role?: string } };
async function request(url: string, method: string, body?: unknown): Promise<ApiResult> { const response = await fetch(url, { method, headers: { 'Content-Type': 'application/json' }, body: body ? JSON.stringify(body) : undefined }); return response.json() as Promise<ApiResult>; }

export function AuthForm({ mode }: { mode: 'login' | 'register' }) {
  const [error, setError] = useState<string>(); const [fieldErrors, setFieldErrors] = useState<Record<string, string[]>>({}); const [pending, setPending] = useState(false); const router = useRouter(); const searchParams = useSearchParams();
  const submit = async (form: HTMLFormElement) => { setPending(true); setError(undefined); const values = Object.fromEntries(new FormData(form)); const result = await request(`/api/auth/${mode}`, 'POST', values); setPending(false); if (!result.success) { setError(result.formError); setFieldErrors(result.fieldErrors ?? {}); return; } const next = searchParams.get('next'); const target = next?.startsWith('/') && !next.startsWith('//') ? next : `/${result.data?.role ?? 'student'}`; router.push(target); router.refresh(); };
  return <form className="auth-form" noValidate onSubmit={(event) => { event.preventDefault(); void submit(event.currentTarget); }}><FormErrorSummary>{error}</FormErrorSummary>{mode === 'register' && <label>Повне ім’я<AppInput name="fullName" placeholder="Ім’я та прізвище" autoComplete="name" /><FieldError>{fieldErrors.fullName?.[0]}</FieldError></label>}<label>Електронна пошта<AppInput name="email" type="email" placeholder="you@example.com" autoComplete="email" /><FieldError>{fieldErrors.email?.[0]}</FieldError></label><label>Пароль<AppInput name="password" type="password" placeholder="Щонайменше 6 символів" autoComplete={mode === 'login' ? 'current-password' : 'new-password'} /><FieldError>{fieldErrors.password?.[0]}</FieldError></label><AppButton type="submit" disabled={pending}>{pending ? 'Зачекайте…' : mode === 'login' ? 'Увійти до StudentFlow' : 'Створити студентський профіль'}</AppButton></form>;
}

export function ApplyDialog({ activityId, activityTitle }: { activityId: string; activityTitle: string }) {
  const [open, setOpen] = useState(false); const [error, setError] = useState<string>(); const [pending, setPending] = useState(false); const { push } = useToast(); const router = useRouter();
  const submit = async (form: HTMLFormElement) => { setPending(true); const result = await request('/api/applications', 'POST', { activityId, motivation: new FormData(form).get('motivation') }); setPending(false); if (!result.success) return setError(result.formError); setOpen(false); push('Крок додано до маршруту.'); router.refresh(); };
  return <><AppButton type="button" onClick={() => setOpen(true)}>Додати в маршрут</AppButton><AppModal open={open} title={`Маршрут: ${activityTitle}`} onClose={() => setOpen(false)}><form noValidate onSubmit={(event) => { event.preventDefault(); void submit(event.currentTarget); }}><FormErrorSummary>{error}</FormErrorSummary><label className="form-label">Навіщо вам цей крок<AppTextarea name="motivation" rows={6} placeholder="Наприклад: хочу закрити прогалину в презентації або додати доказ до портфоліо" /></label><div className="dialog-actions"><AppButton type="button" variant="ghost" onClick={() => setOpen(false)}>Скасувати</AppButton><AppButton type="submit" disabled={pending}>{pending ? 'Додаємо…' : 'Додати крок'}</AppButton></div></form></AppModal></>;
}

export function ReportDialog({ applicationId, title }: { applicationId: string; title: string }) {
  const [open, setOpen] = useState(false); const [error, setError] = useState<string>(); const [pending, setPending] = useState(false); const { push } = useToast(); const router = useRouter();
  const submit = async (form: HTMLFormElement) => { const values = Object.fromEntries(new FormData(form)); setPending(true); const result = await request('/api/reports', 'POST', { applicationId, ...values }); setPending(false); if (!result.success) return setError(result.formError); setOpen(false); push('Доказ передано ментору.'); router.refresh(); };
  return <><AppButton type="button" variant="secondary" onClick={() => setOpen(true)}>Додати доказ</AppButton><AppModal open={open} title={`Доказ: ${title}`} onClose={() => setOpen(false)}><form noValidate onSubmit={(event) => { event.preventDefault(); void submit(event.currentTarget); }}><FormErrorSummary>{error}</FormErrorSummary><label className="form-label">Що підтверджує результат<AppTextarea name="reflection" rows={5} placeholder="Опишіть артефакт, рішення, виступ, файл або інший результат" /></label><label className="form-label">Час роботи<AppInput name="hoursSpent" type="number" placeholder="Наприклад, 4" /></label><label className="form-label">Компетентності<AppInput name="skillsGained" placeholder="Наприклад, презентація, дослідження, командна робота" /></label><div className="dialog-actions"><AppButton type="button" variant="ghost" onClick={() => setOpen(false)}>Скасувати</AppButton><AppButton type="submit" disabled={pending}>{pending ? 'Зберігаємо…' : 'Передати доказ'}</AppButton></div></form></AppModal></>;
}

export function ReviewDialog({ kind, id, title }: { kind: 'application' | 'report'; id: string; title: string }) {
  const [open, setOpen] = useState(false); const [mode, setMode] = useState<'approved' | 'rejected' | 'needs_changes'>('approved'); const [error, setError] = useState<string>(); const [pending, setPending] = useState(false); const { push } = useToast(); const router = useRouter();
  const submit = async (form: HTMLFormElement) => { setPending(true); const result = await request('/api/reviews', 'POST', { kind, id, status: mode, feedback: new FormData(form).get('feedback') }); setPending(false); if (!result.success) return setError(result.formError); setOpen(false); push('Фідбек збережено.'); router.refresh(); };
  return <><AppButton type="button" variant="secondary" onClick={() => setOpen(true)}>Дати фідбек</AppButton><AppModal open={open} title={title} onClose={() => setOpen(false)}><form noValidate onSubmit={(event) => { event.preventDefault(); void submit(event.currentTarget); }}><FormErrorSummary>{error}</FormErrorSummary><div className="decision-grid"><button type="button" className={mode === 'approved' ? 'decision active' : 'decision'} onClick={() => setMode('approved')}>Прийняти</button><button type="button" className={mode === 'needs_changes' ? 'decision active' : 'decision'} onClick={() => setMode('needs_changes')}>{kind === 'report' ? 'Уточнити' : 'Відкласти'}</button><button type="button" className={mode === 'rejected' ? 'decision active' : 'decision'} onClick={() => setMode('rejected')}>Відхилити</button></div><label className="form-label">Коментар для студента<AppTextarea name="feedback" rows={4} placeholder="Додайте конкретний фідбек до маршруту або доказу" /></label><div className="dialog-actions"><AppButton type="button" variant="ghost" onClick={() => setOpen(false)}>Скасувати</AppButton><AppButton type="submit" disabled={pending}>{pending ? 'Зберігаємо…' : 'Зберегти фідбек'}</AppButton></div></form></AppModal></>;
}

export function CancelApplication({ applicationId }: { applicationId: string }) { const [open, setOpen] = useState(false); const [reason, setReason] = useState(''); const [error, setError] = useState<string>(); const { push } = useToast(); const router = useRouter(); const confirm = async () => { const result = await request('/api/applications', 'DELETE', { applicationId, reason }); if (!result.success) return setError(result.formError); setOpen(false); push('Крок прибрано з маршруту.'); router.refresh(); }; return <><AppButton type="button" variant="ghost" onClick={() => setOpen(true)}>Прибрати</AppButton><AppModal open={open} title="Прибрати крок" onClose={() => setOpen(false)}><FormErrorSummary>{error}</FormErrorSummary><label className="form-label">Причина<AppTextarea value={reason} onChange={(event) => setReason(event.target.value)} rows={3} placeholder="Коротко поясніть рішення" /></label><div className="dialog-actions"><AppButton type="button" variant="ghost" onClick={() => setOpen(false)}>Назад</AppButton><AppButton type="button" variant="danger" onClick={() => void confirm()}>Прибрати з маршруту</AppButton></div></AppModal></>; }

export function ToggleUserButton({ profileId, status }: { profileId: string; status: string }) { const [open, setOpen] = useState(false); const { push } = useToast(); const router = useRouter(); const confirm = async () => { const result = await request('/api/admin', 'PATCH', { profileId }); if (!result.success) return push(result.formError ?? 'Не вдалося змінити статус.'); setOpen(false); push('Статус користувача оновлено.'); router.refresh(); }; return <><AppButton type="button" variant={status === 'active' ? 'danger' : 'secondary'} onClick={() => setOpen(true)}>{status === 'active' ? 'Деактивувати' : 'Активувати'}</AppButton><ConfirmDialog open={open} title="Змінити статус користувача" message="Користувач одразу втратить або отримає доступ до робочого простору." confirmText="Підтвердити" danger={status === 'active'} onCancel={() => setOpen(false)} onConfirm={() => void confirm()} /></>; }

export function ProfileForm({ profile, groups, specialities }: { profile: { fullName: string; phone?: string; bio?: string; groupId?: string; specialityId?: string; role: string }; groups: { id: string; name: string }[]; specialities: { id: string; name: string }[] }) { const [error, setError] = useState<string>(); const [pending, setPending] = useState(false); const { push } = useToast(); const router = useRouter(); const submit = async (form: HTMLFormElement) => { setPending(true); const result = await request('/api/profile', 'PATCH', Object.fromEntries(new FormData(form))); setPending(false); if (!result.success) return setError(result.formError); push('Профіль оновлено.'); router.refresh(); }; return <form className="surface" noValidate onSubmit={(event) => { event.preventDefault(); void submit(event.currentTarget); }}><FormErrorSummary>{error}</FormErrorSummary><label className="form-label">Повне ім’я<AppInput name="fullName" defaultValue={profile.fullName} /></label><label className="form-label">Телефон<AppInput name="phone" defaultValue={profile.phone} placeholder="+380 …" /></label>{profile.role === 'student' && <div className="dashboard-grid"><label className="form-label">Група<AppSelect name="groupId" defaultValue={profile.groupId}>{groups.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</AppSelect></label><label className="form-label">Спеціальність<AppSelect name="specialityId" defaultValue={profile.specialityId}>{specialities.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</AppSelect></label></div>}<label className="form-label">Про себе<AppTextarea name="bio" defaultValue={profile.bio} rows={4} placeholder="Коротко про свої цілі" /></label><AppButton type="submit" disabled={pending}>{pending ? 'Зберігаємо…' : 'Зберегти зміни'}</AppButton></form>; }

export function AdminCreateDialog({ kind, teachers = [], categories = [], clubs = [] }: { kind: 'teacher' | 'activity' | 'reference'; teachers?: { id: string; fullName: string }[]; categories?: { id: string; name: string }[]; clubs?: { id: string; name: string }[] }) { const [open, setOpen] = useState(false); const [error, setError] = useState<string>(); const [pending, setPending] = useState(false); const { push } = useToast(); const router = useRouter(); const heading = kind === 'teacher' ? 'Створити ментора' : kind === 'activity' ? 'Створити можливість' : 'Додати довідник'; const submit = async (form: HTMLFormElement) => { setPending(true); const values = Object.fromEntries(new FormData(form)); const result = await request('/api/admin', 'POST', { action: kind, ...values }); setPending(false); if (!result.success) return setError(result.formError); setOpen(false); push('Дані збережено.'); router.refresh(); }; return <><AppButton type="button" onClick={() => setOpen(true)}>Додати</AppButton><AppModal open={open} title={heading} onClose={() => setOpen(false)}><form noValidate onSubmit={(event) => { event.preventDefault(); void submit(event.currentTarget); }}><FormErrorSummary>{error}</FormErrorSummary>{kind === 'teacher' && <><label className="form-label">Повне ім’я<AppInput name="fullName" /></label><label className="form-label">Електронна пошта<AppInput name="email" type="email" /></label><label className="form-label">Початковий пароль<AppInput name="password" type="password" /></label></>}{kind === 'activity' && <><label className="form-label">Назва<AppInput name="title" /></label><label className="form-label">Короткий опис<AppTextarea name="shortDescription" rows={3} /></label><div className="dashboard-grid"><label className="form-label">Трек<AppSelect name="categoryId">{categories.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</AppSelect></label><label className="form-label">Майданчик<AppSelect name="clubId">{clubs.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</AppSelect></label><label className="form-label">Ментор<AppSelect name="teacherId">{teachers.map((item) => <option key={item.id} value={item.id}>{item.fullName}</option>)}</AppSelect></label><label className="form-label">Формат<AppSelect name="format"><option value="offline">Офлайн</option><option value="online">Онлайн</option><option value="hybrid">Гібрид</option></AppSelect></label></div><label className="form-label">Локація<AppInput name="location" /></label><div className="dashboard-grid"><label className="form-label">Старт<AppInput name="startAt" type="datetime-local" /></label><label className="form-label">Фініш<AppInput name="endAt" type="datetime-local" /></label><label className="form-label">Ліміт учасників<AppInput name="maxParticipants" type="number" /></label><label className="form-label">Цінність<AppInput name="points" type="number" /></label></div></>}{kind === 'reference' && <><label className="form-label">Тип<AppSelect name="kind"><option value="groups">Академічна група</option><option value="specialities">Спеціальність</option><option value="clubs">Майданчик</option><option value="categories">Трек</option><option value="badges">Відзнака</option></AppSelect></label><label className="form-label">Назва<AppInput name="name" /></label><label className="form-label">Код або колір<AppInput name="code" placeholder="Наприклад, ІПЗ" /></label><label className="form-label">Опис<AppTextarea name="description" rows={3} /></label></>}<div className="dialog-actions"><AppButton type="button" variant="ghost" onClick={() => setOpen(false)}>Скасувати</AppButton><AppButton type="submit" disabled={pending}>{pending ? 'Зберігаємо…' : 'Створити'}</AppButton></div></form></AppModal></>; }

type CrudField = { name: string; label: string; type?: 'text' | 'email' | 'password' | 'number' | 'textarea' | 'select'; value?: string | number; options?: { value: string; label: string }[] };
export function AdminCrudActions({ entity, id, title, fields }: { entity: string; id: string; title: string; fields: CrudField[] }) {
  const [editOpen, setEditOpen] = useState(false);
  const [deleteOpen, setDeleteOpen] = useState(false);
  const [error, setError] = useState<string>();
  const [pending, setPending] = useState(false);
  const { push } = useToast();
  const router = useRouter();
  const save = async (form: HTMLFormElement) => {
    setPending(true);
    const values = Object.fromEntries(new FormData(form)) as Record<string, string>;
    const result = await request('/api/admin', 'PUT', { entity, id, values });
    setPending(false);
    if (!result.success) return setError(result.formError);
    setEditOpen(false);
    push('Зміни збережено.');
    router.refresh();
  };
  const remove = async () => {
    const result = await request('/api/admin', 'DELETE', { entity, id });
    if (!result.success) return push(result.formError ?? 'Не вдалося видалити запис.');
    setDeleteOpen(false);
    push('Запис і пов’язані дані видалено.');
    router.refresh();
  };
  return <div className="data-row-actions"><AppButton type="button" variant="secondary" onClick={() => setEditOpen(true)}>Редагувати</AppButton><AppButton type="button" variant="danger" onClick={() => setDeleteOpen(true)}>Видалити</AppButton><AppModal open={editOpen} title={`Редагувати: ${title}`} onClose={() => setEditOpen(false)}><form noValidate onSubmit={(event) => { event.preventDefault(); void save(event.currentTarget); }}><FormErrorSummary>{error}</FormErrorSummary>{fields.map((field) => <label className="form-label" key={field.name}>{field.label}{field.type === 'textarea' ? <AppTextarea name={field.name} defaultValue={String(field.value ?? '')} rows={4} /> : field.type === 'select' ? <AppSelect name={field.name} defaultValue={String(field.value ?? '')}>{field.options?.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</AppSelect> : <AppInput name={field.name} type={field.type ?? 'text'} defaultValue={String(field.value ?? '')} />}</label>)}<div className="dialog-actions"><AppButton type="button" variant="ghost" onClick={() => setEditOpen(false)}>Скасувати</AppButton><AppButton type="submit" disabled={pending}>{pending ? 'Зберігаємо…' : 'Зберегти'}</AppButton></div></form></AppModal><ConfirmDialog open={deleteOpen} title={`Видалити: ${title}`} message="Запис буде видалено разом із пов’язаними маршрутами, доказами, відзнаками або медіа, якщо вони залежать від цього запису." confirmText="Видалити" danger onCancel={() => setDeleteOpen(false)} onConfirm={() => void remove()} /></div>;
}
