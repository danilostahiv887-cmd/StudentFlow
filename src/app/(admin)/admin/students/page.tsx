import { requireUser } from '@/server/auth';
import { adminData, makePage } from '@/server/repository';
import { WorkspaceNav } from '@/components/layout/workspace-nav';
import { EmptyState, Pagination, SearchBox, StatusBadge } from '@/components/ui/primitives';
import { AdminCrudActions, ToggleUserButton } from '@/components/features/forms';
import Link from 'next/link';
type Search={search?:string;page?:string};
export default async function AdminStudentsPage({ searchParams }: { searchParams: Promise<Search> }) { await requireUser(['admin']); const query=await searchParams; const {students,database}=await adminData(); const search=query.search?.toLowerCase(); const result=makePage(students.filter((student)=>!search||`${student.fullName} ${student.email}`.toLowerCase().includes(search)),Number(query.page??1)); return <div className="page"><div className="page-intro"><div><p className="eyebrow">Адміністративний контур</p><h1>Студенти</h1></div><p>Керування студентськими профілями та перегляд маршрутів.</p></div><div className="workspace"><WorkspaceNav role="admin" active="/admin/students" /><div className="workspace-main"><section className="surface"><form action="/admin/students" className="catalog-toolbar"><SearchBox defaultValue={query.search} placeholder="Ім’я або пошта" /><button className="button button-secondary" type="submit">Шукати</button></form>{result.items.length?<div className="row-list">{result.items.map((student)=>{const group=database.groups.find((item)=>item.id===student.groupId)?.name||'Не вибрано';return <article className="data-row" key={student.id}><div><b>{student.fullName}</b><small>{student.email} · {group}</small></div><StatusBadge status={student.status}/><small>{student.pointsTotal} балів</small><div className="data-row-actions"><Link className="button button-secondary" href={`/admin/students/${student.id}`}>Маршрут</Link><ToggleUserButton profileId={student.id} status={student.status}/><AdminCrudActions entity="profile" id={student.id} title={student.fullName} fields={[{name:'fullName',label:'Повне ім’я',value:student.fullName},{name:'email',label:'Пошта',type:'email',value:student.email},{name:'password',label:'Новий пароль',type:'password'}]} /></div></article>})}</div>:<EmptyState title="Нічого не знайдено" body="Змініть пошуковий запит."/>}<Pagination page={result.page} pageCount={result.pageCount} total={result.total} pathname="/admin/students" query={{search:query.search}}/></section></div></div></div>; }
