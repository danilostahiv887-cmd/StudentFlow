import { requireUser } from '@/server/auth';
import { adminData } from '@/server/repository';
import { WorkspaceNav } from '@/components/layout/workspace-nav';
import { StatusBadge } from '@/components/ui/primitives';
import { AdminCreateDialog, AdminCrudActions, ToggleUserButton } from '@/components/features/forms';
export default async function AdminTeachersPage() { await requireUser(['admin']); const {teachers,activities}=await adminData(); return <div className="page"><div className="page-intro"><div><p className="eyebrow">Адміністративний контур</p><h1>Ментори</h1></div><AdminCreateDialog kind="teacher" /></div><div className="workspace"><WorkspaceNav role="admin" active="/admin/teachers"/><div className="workspace-main"><section className="surface"><div className="row-list">{teachers.map((teacher)=><article className="data-row" key={teacher.id}><div><b>{teacher.fullName}</b><small>{teacher.email} · {activities.filter((activity)=>activity.teacherId===teacher.id).length} треків</small></div><StatusBadge status={teacher.status}/><small>Роль: ментор</small><div className="data-row-actions"><ToggleUserButton profileId={teacher.id} status={teacher.status}/><AdminCrudActions entity="profile" id={teacher.id} title={teacher.fullName} fields={[{name:'fullName',label:'Повне ім’я',value:teacher.fullName},{name:'email',label:'Пошта',type:'email',value:teacher.email},{name:'password',label:'Новий пароль',type:'password'}]} /></div></article>)}</div></section></div></div></div>; }
