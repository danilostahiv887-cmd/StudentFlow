import { requireUser } from '@/server/auth';
import { adminData } from '@/server/repository';
import { WorkspaceNav } from '@/components/layout/workspace-nav';
import { ActivityCard } from '@/components/ui/primitives';
import { AdminCreateDialog, AdminCrudActions } from '@/components/features/forms';
export default async function AdminActivitiesPage(){await requireUser(['admin']);const {database,activities,teachers}=await adminData();const categoryOptions=database.categories.map((item)=>({value:item.id,label:item.name}));const clubOptions=database.clubs.map((item)=>({value:item.id,label:item.name}));const teacherOptions=teachers.map((item)=>({value:item.id,label:item.fullName}));return <div className="page"><div className="page-intro"><div><p className="eyebrow">Адміністративний контур</p><h1>Можливості</h1></div><AdminCreateDialog kind="activity" teachers={teachers} categories={database.categories} clubs={database.clubs}/></div><div className="workspace"><WorkspaceNav role="admin" active="/admin/activities"/><div className="workspace-main"><section className="surface"><div className="activity-grid">{activities.map((activity)=><div key={activity.id}><ActivityCard activity={activity}/><AdminCrudActions entity="activity" id={activity.id} title={activity.title} fields={[{name:'title',label:'Назва',value:activity.title},{name:'shortDescription',label:'Короткий опис',type:'textarea',value:activity.shortDescription},{name:'description',label:'Повний опис',type:'textarea',value:activity.description},{name:'categoryId',label:'Трек',type:'select',value:activity.categoryId,options:categoryOptions},{name:'clubId',label:'Майданчик',type:'select',value:activity.clubId,options:clubOptions},{name:'teacherId',label:'Ментор',type:'select',value:activity.teacherId,options:teacherOptions},{name:'points',label:'Бали',type:'number',value:activity.points},{name:'maxParticipants',label:'Місткість',type:'number',value:activity.maxParticipants}]} /></div>)}</div></section></div></div></div>}
